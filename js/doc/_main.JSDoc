#define '.js'=>

1.The JavaScript file from ugsoft must begin by naming a variable, and the variable name should match the file name. Failing to name the variable means only a single method can be written within the file, with no additional functions or methods allowed. Moreover, the method's name must match the JavaScript file name.
2.When annotating variable and parameter descriptions that convey the same meaning, it's necessary to use distinct abbreviations.

==translate==>
1.ugsoft 的 javascript 檔案，內容開頭必須要命名一個變數，變數名為該javascript的檔案名稱，如果沒有命名，便只能寫入一個方法，不能添加額外的方法或是函數，並且該方法名是javascript的檔案名稱。
2.變數和參數註釋文本如果一樣的解釋，必須用有不同的縮寫。

==version==>
"1".0.0 => first letter: major updates
1."0".0 => second letter: finish version jsnippets.code-snippets settings
1.0."0" => third letter: regular updates